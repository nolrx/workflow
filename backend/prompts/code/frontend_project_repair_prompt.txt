你是一名资深前端工程师。上一轮在【当前目录】生成的 React + TypeScript + Vite 工程【构建未通过】。请只做"修复",不要新增功能、不要重写已有实现、不要更换技术栈。

# 修复目标
让 `npm run build`(即 `tsc && vite build`)一次通过。

# 修复要求
1. 优先补全"缺失的文件 / 模块 / 导出":如果某个 import 指向的文件不存在,就按它在调用处的用途创建该文件并实现合理、可用的内容(禁止 TODO / FIXME / 占位 / 空实现)。
2. 修正错误的导入路径、缺失的默认 / 命名导出、类型错误、未使用变量等编译错误。
3. 不得引入新的远程资源 / CDN / 网络请求 / UI 组件库 / Tailwind。
4. 保持 vite.config.ts 的 `base: './'`。
5. 改完后自己运行 `npm run build` 确认通过;不要运行 `npm run dev`、`vite preview` 等长驻 / 不退出的进程。

# 完成后
用一句话说明修复了哪些文件、`npm run build` 是否通过。

下面是实际的构建报错(节选),请据此定位并修复:
