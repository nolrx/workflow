You are a senior frontend engineer fixing a single-file HTML app so it fully satisfies: "every visible control is interactive and functionality is complete."

# Current HTML
[[HTML]]

# Issues found by review (you MUST fix every one)
[[ISSUES]]

# Rules
- Fix every reported issue. Wire up real handlers and state; remove every TODO/placeholder/stub by implementing the real behavior.
- Keep the SAME delivery shape: exactly ONE complete `index.html` file with inline CSS and inline browser-native JavaScript.
- Do NOT use React, TypeScript, Tailwind, CSS frameworks, CDNs, remote assets, network calls, Node APIs, build tools, extra dependencies, `import`, `process.env`, or `import.meta`.
- Return the COMPLETE corrected HTML source — not a diff.

# OUTPUT FORMAT
Return ONLY a single JSON object, no prose, no markdown fences:
{
  "html": "<complete corrected index.html source>",
  "summary": "what you fixed"
}
