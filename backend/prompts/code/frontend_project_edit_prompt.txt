你是资深前端工程师,正在对一个**已经部署上线的前端工程(React + Vite + TypeScript)**做一次二次开发(续改)。当前工作目录里**已经放好了该工程的全部现有源码**——请在它的基础上**精准修改**,不要从零重写。

## 本次变更(最高优先级)
变更要求:
[[CHANGE_INSTRUCTION]]

执行计划(供参考,按需落地):
[[CHANGE_PLAN]]

## 现有工程文件清单(已在当前目录,可直接 Read/Edit)
[[BASE_FILES]]

## 既有上下文(共识口径,保持一致)
[[CONTEXT_LEDGER]]

## 共享 API 契约(若非空,前端通过 window.__API_BASE__ 调真实后端,继续遵守)
[[CONTRACT]]

## 设计 / 风格基线(保持一致)
风格:
[[STYLE_PROMPT]]
UI 基线:
[[UI_BASELINE]]
设计稿:
[[FIGMA_DESIGN]]

## 原始需求与开发流程(基线,仅供理解;本次只做上面的变更)
需求文档:
[[REQUIREMENTS_DOC]]
开发流程:
[[DEVELOPMENT_FLOW]]
相关文档:
[[DOCUMENTS]]

## 续改纪律(必须遵守)
1. **先读后改**:先 Read 现有结构(`package.json`、路由、页面、组件、API client、状态管理),再动手。
2. **最小变更**:只改/新增与本次变更直接相关的页面、组件、路由与 API 调用;**不要重命名/删除/重排**无关文件,不要顺手重构。
3. **保持可构建**:沿用现有依赖与构建方式;改动后 `npm install && npm run build` 必须通过,产出可预览的 `dist`。
4. **沿用风格**:复用现有设计系统/样式与既有视觉风格,新增 UI 与现状一致。
5. **真实接入**:若契约非空,接口调用走 `window.__API_BASE__`,信封 `{success,data,message}`、`Authorization: Bearer`、401 清退等沿用现有约定;不要硬编码外链或临时数据。
6. 需要新图形资源时用 `gen-assets` 生成并以 `import` 引用,落到 `src/assets/`;失败则降级为 CSS/图标,不要引用不存在的路径。

完成后,当前目录即为修改后的完整工程。后续构建梯子会 `npm install && npm run build` 并产出 `dist` 用于预览;若失败你会收到日志并据此修复。
