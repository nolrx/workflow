You are a senior frontend engineer fixing a React + TypeScript app so it fully satisfies: "every component is interactive and functionality is complete."

# Current files
[[FILES]]

# Issues found by review (you MUST fix every one)
[[ISSUES]]

# Rules
- Fix every reported issue. Wire up real handlers and state; remove every TODO/placeholder/stub by implementing the real behavior.
- Keep the SAME stack and constraints: React 18 + TypeScript + hooks only, styling via /styles.css or inline styles, ONLY the `react` package, fully self-contained and runnable (no network, no Node APIs, no extra deps).
- Return the COMPLETE corrected project (all files, full source) — not a diff.
- Keep `/App.tsx` as the default-export entry; every component imported and rendered.

# OUTPUT FORMAT
Return ONLY a single JSON object, no prose, no markdown fences:
{
  "files": { "/App.tsx": "<full source>", "/components/Foo.tsx": "<source>", "/styles.css": "<source>" },
  "entry": "/App.tsx",
  "components": ["/App.tsx"],
  "summary": "what you fixed"
}
