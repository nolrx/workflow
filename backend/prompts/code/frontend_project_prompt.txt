你是一名资深前端工程师。请在【当前目录】创建一个 COMPLETE、可运行、可构建的前端工程项目(直接在当前目录初始化,不要再新建一层子目录),实现下面已确认的产品规格。

# 项目共识(内部上下文 — 严格遵守,不得引入与之冲突的内容)
[[CONTEXT_LEDGER]]

# 产品需求
[[REQUIREMENT]]

# 需求文档
[[REQUIREMENTS_DOC]]

# 开发流程
[[DEVELOPMENT_FLOW]]

# 模块 / 文档摘要
[[DOCUMENTS]]

# 视觉风格规格(布局、配色、字体、间距均遵循)
[[STYLE_PROMPT]]

# UI 基调
[[UI_BASELINE]]

# 硬性要求(不可协商;产物会被自动构建校验,违反将被判失败)
1. 技术栈:React 19 + TypeScript + Vite + 纯 CSS(每个组件配套 .css)。禁止 Tailwind、UI 组件库、任何 CDN / 远程资源 / 远程字体 / 网络请求。
2. 工程结构:组件拆到 src/components/;状态用 React hooks;类型集中在 src/types.ts;数据用 localStorage 持久化(首次加载播种合理的示例数据)。
3. vite.config.ts 必须设置 base: './'(相对路径),以便构建产物能在子路径下的 iframe 内预览。
4. 所有交互必须真实可用:每个按钮 / 链接 / 菜单都有真实事件;每个输入 / 选择 / 表单都改变有意义的状态且有提交行为;禁止死控件、禁止 href="#" 空链接。
5. 功能完整:用内存 / localStorage 状态实现真实功能。禁止 TODO / FIXME、占位、"未实现"、"coming soon"、空事件处理器、仅 mock 的界面。
6. 你必须自己依次运行 `npm install` 和 `npm run build` 自检,若有报错就修到两条命令都成功为止。不要运行 `npm run dev`、`vite preview` 或任何长驻 / 不退出的进程。
7. 合理覆盖核心功能,优先完整流程而非装饰性填充。

# 完成后
用一句话总结:生成了哪些主要文件、应用有哪些可交互功能、`npm run build` 是否通过。
